import type { Config } from '../types/configTypes.js'

export const config: Config = {
  application: {},
  reverseProxy: {},
  session: {},
  parserConfigs: {},
  settings: {}
}

export default config
